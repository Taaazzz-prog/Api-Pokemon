import { highlightActiveNav } from '../shared/navigation.js';

document.addEventListener('DOMContentLoaded', () => {
  highlightActiveNav();
  // Placeholder: future initialisations (progression, widgets...)
  console.info('[PTA] Hub initialisé (scaffolding).');
});
